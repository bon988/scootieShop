<div class = "payment">
    <div id="header">
        <h1>Thanks for your order <%= @user.name if !@user.name.blank? %>! </h1>
        <h1>Your payment is confirmed! </h1>
    </div>
    
    <div>Order confirmation number: <%= @order.id %></div>
    
    <div class = "items-container">
        <div class="order">
        <% @total = 0 %>
            <div class = "orderitem">
                <div class = 'image-title'>Item</div>        
                <div class = 'quantity'>Quantity</div>        
                <div class = 'amount'>Amount</div>   
            </div>
        <% @orderitems.each do |id,quantity| %>
            <div class = "orderitem">
                <% item = Orderitem.find_by_id(id) %>
                <div class ="image-title">
                    <div class = "image"><%= image_tag(Item.find_by_id(id).image_url, class: "image-thumbnail") %></div>
                    <div class = "title"><%= item.title %></div>
                </div>
                <div class = "quantity"><%= item.quantity %></div>
                <div class = "amount"><%= number_to_currency(item.quantity * item.price, :unit => "€") %></div>
            </div>
              <% timx = (item.quantity * item.price)%>  
              <% @total += timx %>
          </div>
        <% end %>
        
          <% tax = 30 %>
          <% @overall = tax + @total%>
         <div>
              <div>Tax &euro;30</div>
              <div>You Paid &euro; <%= @overall %></div>
         </div>
    </div>
</div>
