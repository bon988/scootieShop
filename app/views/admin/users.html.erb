<% if (current_user && current_user.admin) || @users.size <= 1 %> 

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>User ID</th>
      <th>Email</th>
      <th>Created At</th>
      <th>Updated At</th>
      <th>Admin Status</th>
      <th>Upgrade to Admin</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td><%= user.created_at %></td>
        <td><%= user.updated_at %></td>
        <% if user.admin %>
            <td>Admin</td>
            <td><a href="admin/downgrade/<%= user.id %>">Downgrade to User</a></td>
        <% else %>
            <td>User</td>
            <td><a href="admin/upgrade/<%= user.id %>">Upgrade to Admin</a></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<% else %>
  <p>Unauthorized User</p>
  <%= link_to "Go to ScootieShop's Homepage", root_path %>
<% end %>
