<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label><%= form.label :title %></label>
    <area><%= form.text_field :title, :class => "inputs" %></area>
  </div>

  <div class="field">
    <label><%= form.label :description %></label>
    <area><%= form.text_area :description, :class => "inputs" %></area>
  </div>

  <div class="field">
    <label><%= form.label :price %></label>
    <area><%= form.text_field :price, :class => "inputs" %></area>
  </div>

  <div class="field">
    <label><%= form.label :image_url %></label>
    <area><%= form.text_field :image_url, :class => "inputs" %></area>
  </div>

  <div class="field">
    <label><%= form.label :category %></label>
    <area><%= form.text_field :category, :class => "inputs" %></area>
  </div>

  <div class="field">
    <label><%= form.label :stock %></label>
    <area><%= form.number_field :stock, :class => "inputs" %></area>
  </div>

  <div class="actions">
    <%= form.submit :class => "buttons" %>
  </div>
<% end %>
